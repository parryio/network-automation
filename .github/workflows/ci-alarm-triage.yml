name: Alarm Triage CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
<<<<<<< HEAD
  alarm-triage:
=======
  triage:
>>>>>>> 04e992d (update)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
<<<<<<< HEAD
        python-version: ["3.11"]
=======
        python: ["3.11"]
      fail-fast: false
    env:
      PYTHONPATH: .
>>>>>>> 04e992d (update)
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
<<<<<<< HEAD
          python-version: ${{ matrix.python-version }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run offline triage
        run: |
          python -m scripts.alarm_triage.triage --alarm demo/alarms/A001.json --out outputs/A001 --offline
      - name: Run tests
        run: |
          python -m pytest -q
      - name: Run batch triage KPI
        run: |
          python -m scripts.alarm_triage.batch --alarms "demo/alarms/*.json" --out outputs/batch --offline
      - name: Upload outputs artifact
        uses: actions/upload-artifact@v4
        with:
          name: alarm-triage-outputs-${{ matrix.os }}
          path: outputs/**
          if-no-files-found: warn
      - name: Upload batch KPI artifact
        uses: actions/upload-artifact@v4
        with:
          name: alarm-triage-kpi-${{ matrix.os }}
          path: outputs/batch/**
          if-no-files-found: warn
      - name: Draft Release (v0.2.1)
        if: matrix.os == 'ubuntu-latest'
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          tag_name: v0.2.1
          name: "Alarm Triage v0.2.1"
          body: |
            See CHANGELOG.md for details.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
=======
          python-version: ${{ matrix.python }}
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install streamlit pillow pytest
      - name: Run triage (offline)
        run: python -m scripts.alarm_triage.triage --alarm demo/alarms/A001.json --out outputs/A001 --offline
      - name: Pytest
        run: python -m pytest -q
      - name: Run batch (offline)
        run: python -m scripts.alarm_triage.batch --alarms "demo/alarms/*.json" --out outputs/batch --offline
      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: alarm-triage-outputs-${{ matrix.os }}
          path: outputs/**
>>>>>>> 04e992d (update)
